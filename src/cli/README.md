Что хочется в cli увидеть:

# MVP
cvgen init --to 
cvgen build --theme [default simple] --from [default json default.json]
# Мысли
cvgen build-html
cvgen build-pdf
cvgen validate --theme [Optinal] --all [Optional], если ничег оне указано. Нужно бросить help по этой опции